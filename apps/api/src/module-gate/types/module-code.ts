// Mesmo conjunto fechado do AddonId em packages/types — os 2 modulos pagos reais do MVP.
export type ModuleCode = 'estoque' | 'financeiro';

export const MODULE_CODES: ModuleCode[] = ['estoque', 'financeiro'];
