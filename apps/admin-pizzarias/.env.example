# URL da API (Sprint 10). Default no codigo (data/api.ts) e' http://localhost:3000 --
# so' precisa deste arquivo se a API rodar em outro lugar. Sem VITE_TENANT_SLUG aqui --
# o platform_superadmin nao tem tenant/subdominio proprio.
VITE_API_URL=http://localhost:3000
